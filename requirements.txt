# 核心科学计算库
numpy>=1.21.0
pandas>=1.3.0
scipy>=1.7.0

# 机器学习库
scikit-learn>=1.0.0
lightgbm>=3.3.0
xgboost>=1.5.0
statsmodels>=0.13.0

# 深度学习框架
# 默认使用 CPU 版 TensorFlow。若需 GPU，请在服务器上先正确安装匹配的 CUDA/cuDNN，
# 然后自行将 tensorflow-cpu 换成合适版本的 tensorflow。
tensorflow-cpu==2.10.1
scikeras==0.10.0

# 基因组数据处理
pandas-plink>=2.2.9

# 实用工具
joblib>=1.1.0
tqdm>=4.62.0
argparse>=1.4.0
contextlib2>=21.6.0

# 可选但推荐的
matplotlib>=3.4.0  # 如需绘图
seaborn>=0.11.0    # 如需绘图
